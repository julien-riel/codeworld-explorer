/// <reference types="vite/client" />

// Types Vite pour les imports spéciaux du bundle (`*?worker`, `import.meta.env`…).
// Nécessaire pour typer les workers Monaco importés en local (voir `ui/monacoEditor.ts`).
